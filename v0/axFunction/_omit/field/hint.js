AxFunction.Extensions.Appkit.FormFactory.prototype.hint = function( content ) {

    var a = this.axFunction.tags;

    // var options = options.hint;
// debugger
return a.hint( content );



    // if ( typeof options === "string" ) {
    //   return a.hint( options );
    // } else if ( typeof options === "object" ) {
    //   return a.hint( options.text, options.attributes );
    // } else {
    //   return null;
    // };

};
