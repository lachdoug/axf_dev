AxFunction.Extensions.Appkit.FormFactory = function( axFunction ) {

  this.axFunction = axFunction;

  this.customFields = new AxFunction.Extensions.Appkit.FormFactory.CustomFields( this );

  // debugger;

};
