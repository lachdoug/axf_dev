App.prototype.appbtn = function( content, onclickFunction, options={} ) {

  var a = this.a;
  var x = this.x;
  // var app = this;

  // options.attributes = options.attributes || {};
  // options.attributes.class = "";

  // debugger

  return a.appbtn( x.btn( content, onclickFunction, options ) )

};
