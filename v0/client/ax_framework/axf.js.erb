// "use strict";

var AxFramework = function () {
  this.tags = new this.Tags( this );
  this.lib = new AxFrameworkLibrary( this );
  this.extensions = new AxFrameworkExtensions( this );
  this.cellBuilder = new this.CellBuilder( this );
};

<% Dir.glob( [ './v0/client/ax_framework/axf/**/*.js' ] ).each do |file| %>
 	<%= File.read file %>
<% end %>
