var inDevelopment=<%= Sinatra::Base.development? %>;
<%= V0::Api::Models::Distribution::Client.distribution %>;
ax( App );

// ax( (a,x) => [
//   a.span("hi"),
//   { $text: "hi" },
//   a.span( { $text: "hi" }, { style: "color: red;" } )
// ] );

// ax("hello world");

// ax( (a,x) => a.h1( x.time() ) );
